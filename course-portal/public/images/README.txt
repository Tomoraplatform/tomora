Place the course banner here as:

  course-banner.png

It is referenced in the right-side hero card on the landing page
(app/page.tsx) via the path /images/course-banner.png.
Recommended: a landscape image (~1271x845 / 3:2). It renders responsively
with rounded corners and a soft border/shadow.
